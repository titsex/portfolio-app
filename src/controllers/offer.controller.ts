import { NextFunction, Request, Response } from 'express'

export class OfferController {
    public static async send(request: Request, response: Response, next: NextFunction) {
        // Todo
    }
}
